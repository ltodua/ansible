---
  - name: "challange"
    hosts: localhost

    tasks:

      - name: "test reachability"
        ping:


      - name: "find module"
        find:
          path: ~/Desktop/ansible/task2
          file_type: file
        become_user: lucian

