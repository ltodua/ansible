---
  - name: "first play"
    hosts: localhost


    tasks:


      - name: "test reachability"
        ping:


      - name: "install git"
        apt:
          name: git
          state: present
        become: yes
        become_user: root